import { combineReducers } from 'redux'

import authetnicationReducer from './app/authentication/authentication.reducer'

const rootReducer = combineReducers({
    authetnicationReducer
})

export default rootReducer
